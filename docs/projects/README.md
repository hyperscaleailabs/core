# Cross-module project publications

Whitepapers and combined publications for projects that span multiple
modules. Single-module project articles live in the owning module
(`<module>/docs/articles/<date>-<project>/`) and cross-reference each other
from their own PRs; a publication lands here only when its subject genuinely
crosses module boundaries.

Empty until the first cross-module project publishes.
