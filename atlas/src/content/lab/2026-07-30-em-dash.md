---
title: "766 em dashes and the rule nobody could see breaking"
description: "A style rule stated since the first commit, never enforced, quietly accumulating exceptions through two integrations. Why the cleanup was not a find-and-replace, and why a passing build was the weakest evidence available."
level: "intermediate"
updated: 2026-07-31
created: 2026-07-30
tags: [lab-notes, sdlc, ci, process, tooling]
module: "sdlc"
project: "em-dash"
articlePath: "sdlc/docs/articles/2026-07-30-em-dash/article.md"
issue: 20
pr: 21
evidenceTier: "process"
draft: false
aiGenerated: false
license: "CC-BY-4.0"
sources:
  - title: "766 em dashes and the rule nobody could see breaking (module article)"
    url: "https://github.com/hyperscaleailabs/core/blob/main/sdlc/docs/articles/2026-07-30-em-dash/article.md"
    publisher: "hsailabs"
    license: "Apache-2.0"
  - title: "Lessons from retiring the em dash"
    url: "https://github.com/hyperscaleailabs/core/blob/main/sdlc/docs/lessons/2026-07-30-em-dash.md"
    publisher: "hsailabs"
    license: "Apache-2.0"
---

> Field notes from the `sdlc` module. Full article, evidence, and review trail in
> [the module article](https://github.com/hyperscaleailabs/core/blob/main/sdlc/docs/articles/2026-07-30-em-dash/article.md).

Our contributor guide has said *plain dash, never an em dash* since the first
commit. When we finally counted, the repository held **766 em dashes across
about 114 files**.

Nobody had ignored the rule. It had no way to be noticed.

## Where they came from

Almost none were typed by someone who had read the rule and disagreed. They
arrived in bulk when two existing codebases were integrated - content written
under a different house style, moved in wholesale.

That is the pattern worth taking away. An integration is the moment a
repository's writing conventions are most likely to break and least likely to be
checked, because that week's review attention is on structure, licensing, module
boundaries, and CI. Prose conventions slip through a review whose checklist is
about architecture.

The count was never a measure of carelessness. It measured how long an unguarded
rule had been accumulating exceptions.

## Why it was not a find-and-replace

**A line-leading em dash is a list item.** Three occurrences were wrapped
sentence continuations:

```markdown
This is the mechanism behind the entire [agent loop](/foundations/what-is-an-agent)
— the atlas covers its design tradeoffs in depth under [tool use](/patterns/tool-use).
```

Replace that in place and Markdown renders a **bullet** where there was a
sentence. The build stays green, the content schema still validates, and only a
reader notices. The transformer was written to refuse line-leading dashes and
report them, turning a silent rendering bug into three lines of manual reflow.

**Sometimes the character is data, not punctuation.** A shipped MVP used `'—'` as
a table value meaning *not applicable*, rendered to an operator. A docs page
documented `license: "—"` as the marker for an unknown source license - which a
blind replace turned into `license: " - "`, a string that is not a value at all
but still parses. That second shape is the dangerous one, because the result is
still syntactically valid.

**Fallback rules leave artifacts.** One template held `>— {v}<` and came out as
`> -  {v}`, with a doubled space. HTML collapses it, so nothing looked wrong on
the page.

## The part worth generalizing

Every automated gate passed on the first run: type checks clean, 50 pages built,
corpus schema valid, 61 tests green, golden release decisions reproducing their
baseline.

None of it would have caught a stray bullet, a doubled space, or a placeholder
turned into punctuation. All three real defects were found by reading rendered
output - two by inspecting the reflowed sentences on the running site, one by
diffing the change for the artifacts the transformer itself could produce.

For a change whose entire surface is prose, a passing build is the weakest
evidence available. It proves the page compiles. It does not prove it reads.

## Ordering the guard

The guard shipped in the same pull request that made the tree clean, deliberately
and in that order. A guard added while the tree is still red fails on its
author's next unrelated change, and the lesson people take from that is to bypass
guards. A cleanup that ships without a guard starts decaying the moment it
merges.

It scans tracked *and* non-ignored untracked files, so a brand-new document fails
in the terminal rather than in CI - and it carries a second check that fails when
one of its own exclusions stops pointing at anything, because an exclusion for a
path that has moved is a rule nobody can see is dead.
