# Copy to .env for local pipeline runs. Never commit real keys.

# Optional: enables model-drafted articles in the ingestion pipeline.
# Without it, the pipeline emits structured outline stubs.
ANTHROPIC_API_KEY=

# Optional: overrides the production site URL used for canonical/sitemap/RSS.
SITE_URL=https://agentic-atlas.vercel.app
