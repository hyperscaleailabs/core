# Agents

Agent services: API services for specific agents plus LiveKit agents that plug into Meet.

- Starts with a text agent, extends to a voice agent (Ultravox plus Kokoro direction).
- Goal: a generalist agent pipeline, pluggable and configurable into the Meet
  conferencing system.
- Exposed as APIs behind API keys where applicable.

Status: placeholder. Structure and code migrate here in upcoming iterations.
See the root [README](../README.md) for repository rules (public repo: no PII, squash merges, policy checks required).
